
Class: 

A class is a blueprint for creating objects -- things that have both data (attributes) and 
behaviour (moethods). A function is just one piece of behaviour.

__init__ = runs AUTOMATICALLY when an object is created (instantiated).
           You don't call it — Python calls it for you.
           This is where attributes (self.x = ...) are set.

self:

  Formal definition:
  "self is a reference to the current instance of the class"

  Simple definition:
  "self = jo box abhi active hai, usi ka naam/pointer"
  or 
  "self ek name/pointer hai us specific box ke liye jo abhi is waqt active hai -
  chahe wo ban raha ho (__init__) ya to koi method call kar raha ho"

  In __init__:
    self = naya bana hua box (jo abhi store ho raha hai)

  In any method:
    self = jo bhi object ne method call kiya
    Example: my_dog.describe()  →  self = my_dog ka box
             my_dog2.describe() →  self = my_dog2 ka box

  Key rule:
    Jo bhi [.]  ke LEFT SIDE pe hota hai — wahi self ban jaata hai method ke andar.
    [object] . [method]()
       ↓
      self

  self aur my_dog same address kyun?
    Because they are TWO NAMES for the SAME box — jaise b = a mein b aur a same hote hain.
    Naya box nahi banta — sirf ek aur naam usi box pe lag jaata hai.

  Every method inside a class MUST have self as first parameter.
  Python automatically pass karta hai — tune kabhi manually nahi pass kiya.

----------------------------------------------------------------------
PROCESS: Jab my_dog = Dog("brunno", "labra") likhte ho — kya hota hai?
----------------------------------------------------------------------

  Step 1: Python dekha → "Dog class hai" → memory mein ek NAYA EMPTY BOX banaya
          Box ka address: 534544 (koi bhi number ho sakta hai)
          Box andar se: [empty]

  Step 2: Python ne AUTOMATICALLY __init__ call kiya
          __init__(self=box_534544, name="brunno", breed="labra")
          self = woh naya empty box (address 534544)

  Step 3: __init__ ke andar line by line chali:
          self.name = "brunno"  →  box 534544 mein name:"brunno" daala
          self.breed = "labra"  →  box 534544 mein breed:"labra" daala
          Box ab: [name:"brunno", breed:"labra"]

  Step 4: __init__ khatam. self ka kaam khatam. self gone.
          Filled box "my_dog" variable mein save ho gaya.
          my_dog → box 534544 [name:"brunno", breed:"labra"]

  Step 5: Jab my_dog.describe() call hota hai:
          Python dekhta hai: "my_dog ne describe() call kiya"
          self = my_dog (jo bhi left side pe tha)
          self.name → my_dog ke box se "brunno" nikala

  Step 6: Doosra object banana:
          my_dog2 = Dog("tommy", "labra")
          → NAYA box bana (address: 534864) → ALAG box
          → self ab 534864 tha (534544 nahi)
          → my_dog aur my_dog2 dono ALAG boxes hain memory mein

KEY INSIGHT:
  class   = blueprint (sirf rules, koi box nahi)
  object  = ek real box in memory with actual data
  __init__ = jo code box banana ke waqt automatically chalta hai
  self    = us box ka temporary naam (sirf method ke andar)
  my_dog  = us same box ka permanent naam (bahar ke liye)



@dataclass = decorator hai 
                __inti__ and __repr__ automatic bana deta hai 

----------------------------------------------------------------------
ASYNC PYTHON
----------------------------------------------------------------------

WHY ASYNC?
  Sync: ek ke baad ek — 10 users × 1 sec = 10 seconds
  Async: sab ek saath — 10 users × 1 sec = 1 second
  
  Kab use karo: jab network wait ho (API calls, database, file read)
  Kab mat karo: heavy CPU calculation mein (async wahan help nahi karta)

KEYWORDS:

  async def  = "ye function await use kar sakta hai"
               async def get_joke() -> str:

  await      = "yahaan wait karo, par doosre tasks mat roko"
               response = await client.get(url)
               Without await: blocking — sab ruk jaata hai
               With await: Python doosra kaam karta hai wait ke dauran

  asyncio.gather()  = "ye sab ek saath chalao"
               results = await asyncio.gather(task1(), task2(), task3())
               Shortcut for many tasks: *[func(i) for i in range(10)]

  asyncio.run()  = "async world mein ghuso" — sirf ek baar, sabse bahar
               asyncio.run(main())

  async with  = context manager jo automatically resource close karta hai
               async with httpx.AsyncClient() as client:

HTTPX — Async HTTP Client:
  import httpx
  async with httpx.AsyncClient(headers={...}) as client:
      response = await client.get(url, timeout=5.0)
      response.raise_for_status()   # 4xx/5xx pe error throw karo
      data = response.json()

ERROR HANDLING in async:
  try:
      response = await client.get(url, timeout=5.0)
      response.raise_for_status()
  except httpx.TimeoutException:    # request time limit cross kiya
      return "timeout error"
  except httpx.HTTPStatusError as e:  # 404, 500 etc
      return f"HTTP error: {e.response.status_code}"
  except Exception as e:            # koi bhi unexpected error
      return f"Unknown error: {str(e)}"

REAL AI USE:
  Har OpenAI/Anthropic/Gemini API call = network wait
  await use karo → ek saath 100 users handle kar sakte ho
  async def ask_gpt(question: str) -> str:
      response = await client.chat.completions.create(...)
      return response.choices[0].message.content





Numpy in Ai ? 
because it is built in C internally
it is fast as C speed
it is faster than list 2


dot product = how much simiar two vectors 
but dot product me ek problem hai 
vector ka size ( lenghth) affect karta hai score 

cosine similarity usko normalize karta hai 
np.linalg.norm(a) = vector ki length (magnitude)

0 to 1 
1 = identical 
0 = completely different

1536 dimentions hote hai 






GRADIENT DESCENT 

Matlab model mistake karke usko fix karke improve karta hai 
taaki aur mistake naa ho 

loss = kitna mistake hai 
m = model ka setting 
isi ko change karte hai 

apoch = kitni maar model mistake ko fix karta hai ( rounds )
learning rate = model kitna speed se mistake ko fix karta hai 

gradient = slope of loss 
yaani ki kis direction me chale to loss badhega ya ghatega 
ham always slopeke against vaali direction jaate hai
kyuki loss ko kam karna hai jitna ho sake 




.reshape(-1, 1) 
sklearn ko data column format me chahiye 
-1 matlab raw ka automatic count karta hai 
1 matlab 1 column 

(1,-1) matlab 1 raw aur columns automatic count karle 
(2, -1) matlab 2 raw aur columns automatic count karle 
(-1, 4) matlab 4 columns aur raw automatic count karle 

isse 2D milta hai 




coef_ matlab coefficients 
= weigths = m 


incercept_ matlab graph pe line Y-axix ko kaha kaatti hai 

y = 5 * hours + 48
x = 0 hours => y = 48 

this is bias 
and this is incercept_ 

both is same 

incercept means bias 




MAE means mean of absolute erros 
this is not avrage of lossess 
becasue in gradient decsent losses = ( actual - predicted )square 
and in the MAE = actual - predicted erros ka average ( not squared ) 

MAE = average error in original units 











train test split 
mujhe ye already pata hai 

80% ko train kiya 
20% pe test kiya  

X= inputs  
y = outputs

ye original data hai 

ab X inputs me se 80% jo hai vo hoga X_train 
aur y outuputs me 80% jo hai vo hoga Y_train 

aur 

x me baaki bacha 20% jo hai vo X_test 
y me baaki bacha 20% jo hai vo Y_test 


ham jo hai vo X_train aur Y_train ko model ko denge 
vo gradient descent karega internally 
aur m aur b ko imrpove karega 

ab ham predict karenge 

X_test denge 
Y_test actual values hai jo already hamare pass hai 

model answer predict karega 
usko Y_test jo already hamare pass hai 
uske sath compare karega 

R² aur MAE se 







Y = mX + b 

isse m aur b find kiya jaata hai aur usko optimize kiya jata hai 
taaki minimum loss aaye   

ye chiz .fit() ke time hoti hai 

ab predict ke time pe Y = mX + b me values put karke output milta hai 







JUst for notes 

AI -> MAchiens jo intellignet kaam kare 
      ( chess palying, image recognition, chatbots)

ML -> AI ka ek subset 
      data se pattern seekhna - gradient descent
      "Expilicitly program mat karo, data se seekhne do"

DL -> ML ka ek subset 
      neural metworks use karta hai
      bahut saare layers -> "deep" learning

LLM -> DL ka ek subset
       text pe trained, massive sclae 
       GPT, gemini, claude = LLMs

    
AI 
\______ ML ( mene kiya - gradient descent , scikit-learn )
         \______DL ( neural netowrk, pytorch)
                 \______ LLM ( open AI , real chatbots )






Pytorch vs Scikit-learn

scikit-learn - ready made models 
                models.fit() -> done 
                Simple Ml ke liye 

PyTorch -> building blocks deta hai 
            me khud neural network banata hu 
            Research + production AI ke liye 
            GPT, stable diffusion - sab pytorch me bane hai 





Tensor = Numpy aaray + GPU support + gradient tracking 

numpy_array = np.array([1,2,3])   CPU only, gradient tracking nahi 
torch_tensor = torch.tensor([1,2,3])    GPU pe ja sakta hai + gradient track karta hai  

Gradient tracking = model sikhta kaise hai 
ye wahi hai jo mene manuaally kiya tha 20 Aug ko

score = m * hours + b
predictions = m * hours + b  

neural netowrk = bohot saare m aur b ek ke baad ek 
Input -> [ m1 *x + b1] -> [m2 *x + b2] -> [m3 *x + b3] -> output 
          layer 1         layer 2         layer 3     

har layer uske pehle layer ki output leti hai 
is tarah complex pattern seekh sakta hai 
jo simple y = mx + b nahi sikh sakta 

GPT-4 = bohot saari layers * bohot sare m aur b = 1.8 trillion parameters



# NumPy — sirf ek kaam karta hai: math
a = np.array([1.0, 2.0, 3.0])

# PyTorch tensor — 3 kaam karta hai:
t = torch.tensor([1.0, 2.0, 3.0])
# 1. Math (same as numpy) ✅
# 2. GPU pe bhejna ✅ → t.cuda()
# 3. Gradient yaad rakhna ✅ → requires_grad=True



gradient yaad rakhta hai 
mene manually likha tha 
gradient_m = (2/n) * np.sum((predictions - scores) * hours)
m = m - learning_rate * gradient_m

pytorch khud se karta hai ye 
loss.backward() bolke 


GPU pe chalta hai ka kya matlab hua ? 

CPU = Computer ka main processor (Intel, AMD)

8-16 cores
Ek complex kaam bahut achi tarah karta hai
Sequential tasks ke liye best
GPU = Graphics card ka processor (NVIDIA, AMD)

Thousands of cores (RTX 3080 = 8704 cores!)
Ek ek core simple hai, par sab ek saath kaam karte hain
Parallel math ke liye best

Neural Network Mein Kyun GPU?
Neural network training = millions of small multiplications ek saath.

Layer ka ek step:
[1000 inputs] × [weights matrix 1000×3072] = result
= 30 LAKH multiplications ek baar mein
CPU pe: 16 cores → thoda thoda karke → slow GPU pe: 8704 cores → sab ek saath → 100x faster

GPT-4 training mein thousands of NVIDIA A100 GPUs use hue — 3 months ke liye non-stop.




AUTOGRAD
requires_grad=True → "is tensor ka gradient track karo"
loss.backward() → PyTorch khud gradient calculate karta hai
ye wahi kaam hai jo maine manually kiya tha pehle 

gradient_m = (2/n) * np.sum(...)  ← 
manual formula
loss.backward()                   ← PyTorch ka automatic


TRAINING LOOP PATTERN — ye hamesha same rahega
optimizer.zero_grad()   # purana gradient clear karo
loss.backward()         # gradient calculate karo
optimizer.step()        # weights update karo


NEURAL NETWORK (nn.Module)
nn.Linear(1, 8)   → 1 input, 8 neurons output (8 weights + 8 biases = 16 parameters)
nn.ReLU()         → negative → 0, positive → as-is (bina iske sirf y=mx+b hoga)
nn.Linear(8, 1)   → 8 input, 1 output

ReLU ke bina neural network complex patterns nahi seekh sakta
ReLU = "non-linearity" jo model ko power deti hai

Adam optimizer = smart gradient descent
learning rate khud adjust karta hai (regular gradient descent se better)


BCELoss = Binary Cross Entropy Loss
yes/no problems ke liye (malignant/benign, pass/fail, spam/not spam)
MSELoss = regression ke liye (salary, score predict karna)

nn.Sigmoid() = output 0 to 1 ke beech laata hai (probability)
0.5 se zyada → 1 (yes), 0.5 se kam → 0 (no)

StandardScaler = features ko same scale pe laata hai
bina iske neural network properly nahi seekhta
kyunki ek feature 1000s mein ho aur doosra 0.001 mein → imbalance

Cancer Classifier result: 96.5% accuracy
30 features → 2 classes (malignant/benign)
Real medical data pe neural network doctors se better




1. Gradient descent 
->  loss ko minimize karne ke liye , gradient (slope of loss) calculate
    karo -> opposite direction mein m aur b ko thoda adjust karo -> repeat. 
    har round mein loss thoda kam hota hai.
    opposite direction isiliye kyuki loss ghatana hai , gradient loss badhne 
    ki direction batata hai

2. Neural network aur simple y=mx+b mein kya fark hai?
->  Neural network me bohot saari layers hoti hai bohot sare parameteres hote hai 
    jabki y=mx+b ek simple linear function hai 
    lekin sirf multiple layers hone se koi extra benifit nahi milta 
    agar activation function jaise ReLU use naa karo 
    to multiple layers bhi y=mx+b ke equivalent ho sakti hai 
    ReLU non linearity introduce karta hai 
    isi ki wajah se network complex pattern seekh sakta hai 
    jo straight line nahi seekh sakti

3. Training loop mein ye 3 lines kyu hain — order kyu yahi hai?
optimizer.zero_grad()
loss.backward()
optimizer.step()
->  ye teeno chiz ek order me hai 
    reset -> calculate -> apply 
    optimizer.zero_grad() =  gradient ko reset karta hai 
                              kyuki pytorch gradient accumulate karta hai 
                              purana gradient hata do warna galat result 
    loss.backward() = gradient calculate karta hai 
                      parameteres abhi update nahi hue 
                      sirf "kitna adjust karna hai" vo figure our karta hai 
    optimizer.step() = ab parameteres (m,b) actually update hote hai 
                        gradient use karke adjust karo 
    backward() sirf math karta hai 
    actual change step() karta hai 
           
4. Tensor aur NumPy array mein fark
->  tensor gradient automatically track karta hai autograd se 
    numpy nahi karta 
    loss.backward() tensor pe hi kaam karta hai 
    numpy pe nahi 
    tensor GPU pe chal sakta hai 
    numpy nahi vo sirf normal CPU ke liye 



generate_content = ek sawaal , ek jawab - Ai kuch yaad nhi rakhta
chats.create = conversation -- Ai yaad rakhta hai 
System instruction = AI ka character define karta hai - same model alag behaviour


DAY 8 — Embeddings

Embedding = text ko numbers mein convert karna jisme meaning capture hoti hai
"car" → [0.2, 0.8, ...] (3072 numbers)
similar meaning → similar numbers → similar position in space

car - automobile  = 0.830  (same cheez)
car - vehicle     = 0.869  (broad category)
car - apple       = 0.623  (bilkul alag)

cosine similarity = 2 embeddings kitni similar hain (0 = alag, 1 = same)
formula: dot_product / (norm_a * norm_b)

Semantic Search = query ki embedding nikalo → sab documents se compare karo
→ highest similarity wala document = best match

Use cases:
- RAG mein relevant documents dhundhna
- Recommendations
- "car accident" search karo → "vehicle crash" bhi mile